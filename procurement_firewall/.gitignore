# runtime audit output from the CLI (regenerated per run)
audit_log.jsonl
*.audit.jsonl

# python
__pycache__/
*.pyc
.pytest_cache/

# LLM judge response cache (reproducible from the API; not a deliverable)
eval/.judge_cache.json
eval/.judge_cache.json.tmp

# NOTE: eval/runs/*.json and eval/runs/history.jsonl ARE committed on purpose —
# the persisted eval numbers are part of the deliverable.
